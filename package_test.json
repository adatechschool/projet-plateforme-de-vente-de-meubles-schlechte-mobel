<script>
import { ref } from 'vue' ## pas besoin
import { supabase } from '@/supabase';

export default {
 name: 'furnitureItemPics', ## or 'FurnitureItemPics'
setup() {
    const fileInput = ref(null)
    const imageURL = ref(null)
    const showOverlay = ref(false)

    const handleFileInputChange = async (event) => {
        const file = even.target.files[0]
        if (!file) return

        showOverlay.value = true
        const { data, error } = await supabase.storage.from('avatars').upload
        ('avatar_${Date.now()}.png', 
            file
        )
        showOverlay.value = false
        if (error) {
            console.error(error)
            return
        }
        imageURL.value = URL.createObjectURL(file)
        }

        return {
            fileInput,
            imageURL,
            showOverlay,
            handleFileInputChange,
        }
    },
}
</script>

<template>
<div class="relative w-24 h-24 overflow-hidden border-2 border-gray-300 rounded-full cursor-pointer">
<img v-if="imageURL" :src="imageURL" class="object-cover w-full h-full">
<div v-else class="flex items-center justify-center w-full h-full text-gray-500 cursor-pointer">
<svg xmlns="https://www.schelchtemobel.com" "http://www.w3.org/2000/svg" class="w-8 h-8" viewBox="0 0 20 20" fill="currentColor">
<path fill-rule="evenodd" d="M10 0&10 10 0 1 0 10 10A10 10 0 0 0 10 0xM9 9H5v2h4v4h2v-4h4V9h-4V5H9v4z"
    clip-rule="evenodd" />
</svg>
</div>
<label for="avatar-upload" class="absolute inset-0 opacity ... 
cursor-pointer">
    <span class="sr-only"> Upload avatar </span>
    <input id="avatar-upload" type="file" @change="handle" 
    class="w-full h-full">
</label>
<div v-if="showOverlay" class="absolute inset-0 flex item 
justify-center bg-gray-500 opacity-50 cursor-pointer">
<svg xmlns="https://www.schelchtemobel.com" "http://www.w3.org/2000/svg" class="w-8 h-8" "animate-pulse" viewBox="0 0 20 20"
    fill="currentColor">
    <path fill-rule="evenodd" d="M10 0a10 10 0 1 0 10 10 0zM9 9H5v2h4v4h2v-4h4V9h-4V5H9v4z"
        clip-rule="evenodd" />
    </svg>
</div>